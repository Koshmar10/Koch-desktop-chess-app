[package]
name = "koch-engine"
version = "0.1.0"
edition = "2021"
description = "Chess engine core: board representation, move generation, rules. No Tauri/DB/IO dependencies."

[dependencies]
serde = { version = "1", features = ["derive"] }
ts-rs = "11"
